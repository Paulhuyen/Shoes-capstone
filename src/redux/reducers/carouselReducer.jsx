import { createSlice } from '@reduxjs/toolkit'

const initialState = {
          carousel: [   ],
}

const carouselReducer = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = carouselReducer.actions

export default carouselReducer.reducer